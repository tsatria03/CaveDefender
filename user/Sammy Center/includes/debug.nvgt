void debug(string text, bool output=false)
{
if (output==false)
{
alert("debug", text);
}
else
{
int answer=question("debug", text+". Would you like to copy the output?");
if (answer==1)
{
clipboard_copy_text(text);
}
}
}